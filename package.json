{
  "name": "fraud-detection-prisma",
  "version": "1.0.0",
  "private": true,
  "description": "Prisma schema & migrations for the Supabase PostgreSQL database. Schema/migration tooling only — the Python app uses SQLAlchemy + pandas for runtime data access.",
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:deploy": "prisma migrate deploy",
    "prisma:status": "prisma migrate status",
    "prisma:studio": "prisma studio"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
